<!--詳細画面-->
<div id="wrapper">

  <button class="button button--outline">いいね！</button>
  <div class="ad_detail_img"><%= image_tag @ad.image.url(:thumb) %></div>
  <%= @ad.client.name %>
  <%= @ad.title %>
  <%= render partial: "footer" %>

</div>
